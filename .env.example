# Copy this file to .env and fill in your own values before running in production.

# A long random string used to sign login sessions. Change this to something unique.
JWT_SECRET=change-this-to-a-long-random-string

# Port the server runs on (most hosts set this automatically)
PORT=3000

# Set to "production" when deployed online
NODE_ENV=production
